/**
 * Where a build that cannot replace itself sends the user instead: unsigned or dmg/pkg macOS,
 * a deb/rpm install, an msi install, or a development run.
 */
export const releasesPageUrl = "https://github.com/GlobalArtInc/k8sight/releases";
